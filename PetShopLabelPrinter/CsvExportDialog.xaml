<Window x:Class="PetShopLabelPrinter.CsvExportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Export do CSV" Height="180" Width="420" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Export aktuálneho zoznamu produktov do CSV súboru." Margin="0,0,0,12" TextWrapping="Wrap"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Oddeľovač:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="CmbDelimiter" Width="120">
                <ComboBoxItem Content="Bodkočiarka (;)" Tag=";" IsSelected="True"/>
                <ComboBoxItem Content="Čiarka (,)" Tag=","/>
            </ComboBox>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="BtnExport" Content="Exportovať..." Click="BtnExport_Click" IsDefault="True" Padding="16,8" Margin="0,0,8,0"/>
            <Button x:Name="BtnCancel" Content="Zrušiť" IsCancel="True" Click="BtnCancel_Click" Padding="16,8"/>
        </StackPanel>
    </Grid>
</Window>
